spring.application.name=discover-service

server.port = 8761

# This tells eureka client not to register itself with the eureka server and not
# to fetch the registry from eureka server because it's the server itself not the client.
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
